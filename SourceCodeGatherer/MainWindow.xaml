<Window x:Class="SourceCodeGatherer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Source Code Gatherer" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Root Directory Selection -->
        <GroupBox Grid.Row="0" Header="Source Directory" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Root Path:"/>
                <TextBox Grid.Column="1" Name="txtRootPath" Margin="5,0"
                         Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Content="Browse..." 
                        Command="{Binding BrowseRootCommand}"/>
            </Grid>
        </GroupBox>

        <!-- File Types Selection -->
        <GroupBox Grid.Row="1" Header="File Types" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Select file types to include:" 
                           Margin="0,0,0,5"/>
                <Button Grid.Row="1" Content="Scan Directory" 
                        Command="{Binding ScanDirectoryCommand}"
                        HorizontalAlignment="Left"/>
            </Grid>
        </GroupBox>

        <!-- File Extensions List -->
        <GroupBox Grid.Row="2" Header="Available File Extensions" Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                <ItemsControl ItemsSource="{Binding FileExtensions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Extension}" 
                                      IsChecked="{Binding IsChecked}"
                                      Margin="5,2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Output File Selection -->
        <GroupBox Grid.Row="3" Header="Output File" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" Content="Output Path:"/>
                <TextBox Grid.Column="1" Name="txtOutputPath" Margin="5,0"
                         Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Content="Browse..." 
                        Command="{Binding BrowseOutputCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="4" Height="20" Margin="0,0,0,10"
                     IsIndeterminate="{Binding IsProcessing}"
                     Visibility="{Binding ProgressVisibility}"/>

        <!-- Action Buttons -->
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" 
                       VerticalAlignment="Center" Foreground="Green"/>
            <Button Grid.Column="1" Content="Export" 
                    Command="{Binding ExportCommand}"
                    IsEnabled="{Binding CanExport}"/>
            <Button Grid.Column="2" Content="Close" 
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>